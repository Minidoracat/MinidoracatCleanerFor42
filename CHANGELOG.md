# Changelog

所有重要的變更都會記錄在此檔案中。

格式基於 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.1.0/)，版本號遵循 `{PZ版本}-{主版本}.{次版本}.{修訂}` 格式。

## [42.20.2-0.2.2] - 2026-08-09

### 修正

- **滑鼠移到電力條／流體條上會噴 Lua 錯誤**：物品提示的擴充假設 `ISToolTipInv.item` 一定是 `InventoryItem`，但原版另有兩處把別的東西塞進同一個 tooltip——`ISEnergyBar.lua:90` 傳電力資源、`ISFluidBar.lua:220` 傳流體容器，而 `ISToolTipInv.lua:187` 是原樣存入不檢查型別。對它們呼叫 `hasModData` 會拋出「Object tried to call nil」並中斷整個 tooltip 繪製。**此問題自 0.1.0 首發即存在**，與 0.2.x 的改動無關；關閉「記錄物品丟棄者」的伺服器不受影響（該情況下不會掛載提示擴充）。

### 效能

- **批次手動刪除不再重複掃描周遭**：舊版對「每一件」要刪的物品都重新搜尋一次周遭——玩家背包（含巢狀袋）加上 9 格範圍內的地板物品、地板袋、家具容器與載具零件容器，而且每個容器要走訪兩次。一次刪 90 件就等於把同一份掃描做 90 遍，且全部擠在伺服器主執行緒的同一次回呼裡（手動刪除沒有像自動清理那樣的每 tick 預算）。改為先建一次「玩家此刻搆得到的所有物品」索引再查表，成本從 O(件數 × 掃描) 降為 O(掃描 ＋ 件數)——刪 90 件的開銷降到與過去刪 1 件相當。

  可及性判定（保險屋 loot 權限、阻隔檢查、載具零件可存取、切比雪夫範圍）語意完全不變，只是從「每格每件各驗一次」變成「每格驗一次」。索引帶著「這次要找哪些 id」建立，且**只替被點名的 id 配置記錄、全部到齊即停止**——因此「右鍵刪自己背包裡的一件東西」在背包階段就收工完全不碰世界，而配置量由手動刪除上限（100 件）封頂，不隨範圍內堆了幾萬件而放大。

- **動物名稱索引不再每輪重建三次**：`getAnimalGroupSet`、`getAnimalLimitOverrides`、`getAnimalZoneLimitOverrides` 三個函式各自重建一份群組名稱索引（走訪全部物種、每個物種呼叫一次 `getText`），因此每輪動物掃描要建三遍——其中兩遍還是為了內容為空的覆寫選項而白做。改為建一次即快取（`AnimalDefinitions` 與端語言在執行期都不會變），並在覆寫選項留空時直接短路。

> 技術要點：配置上限這道防線不是理論潔癖——Kahlua 的每個 Lua table 都是獨立的 `KahluaTableImpl`，底層是 `LinkedHashMap`（`J2SEPlatform.java:35`、`KahluaTableImpl.java:18`），遠比標準 Lua 的輕量 table 昂貴；以 42.20.2 實測 10 萬筆三欄記錄約佔 35 MiB。而 `ItemNumbersLimitPerContainer` 預設 0（無上限，`ServerOptions.java:164`）、`ChickenFeather` 之類的物品重量僅 0.001，單一容器堆到數萬件是做得到的。若不限制配置，光是一個不存在的物品 ID 就能讓伺服器替範圍內每一件物品各配一個 record，且 250ms 節流仍允許每秒約四次。
>
> 舊 `findInContainer` 先呼叫 `getItemWithID` 再呼叫 `getItemWithIDRecursiv`，但反編譯可見後者的迴圈裡就檢查了 `item.id == id`（`ItemContainer.java:3065-3092`），完全涵蓋前者——查無此物時第一次呼叫是純浪費。改寫後 `findAccessibleItem`／`findOnSquare`／`containerResult`／`findInContainer` 一併移除。`scripts/smoke_scanner.lua` 擴充為四個情境、共 24 項斷言：雙桶自動清理、批次刪除的可及性邊界（範圍外／被牆阻隔／最愛物品皆不得被刪）、提早退出（只刪背包內物品時不得掃描世界，但也不得因此略過把關）、拒絕路徑與配置上限（保險屋拒絕、無所在格、只點名不存在的 id 時配置零記錄）。

## [42.20.2-0.2.1] - 2026-08-09

### 變更

- **頭上的警告不再持續顯示**：原本每 3 秒重掛一次 halo、一路撐到下一輪掃描（因為原版 halo 只顯示約 2 秒，而且會被經驗值等其他 halo 蓋掉），現在改成顯示一次約 4 秒後自然消失。聊天室的紅色系統訊息本來就不會被蓋掉、也能回頭翻閱，頭上長掛只是持續佔用視線。清理完成的綠色通知同樣改為一次性。

- **動物警告改為顯示翻譯後的群組名**：原本只有物品名會依客戶端語言翻譯，動物一律顯示原始群組 key（`rat`、`chicken`）。原版沒有可用的鍵——只有分性別年齡的 `IGUI_AnimalType_*`（`rat`→公老鼠、`ratfemale`→母老鼠），對應不到「群組」——因此改用本 MOD 自己的鍵，涵蓋老鼠／家鼠／兔子／雞／牛／豬／羊／鹿／火雞／浣熊。查不到的群組（其他 MOD 新增的）會 fallback 回原名，不會變成空字串。

> 技術要點：連帶移除客戶端的 `OnTick` 回呼——它原本每幀都要檢查警告狀態，現在完全不需要。Client 端淨減 37 行。警告文字一律在**客戶端**組成（伺服器只送 `{kind, detail, count, limit}` 這類原始 key，不送顯示文字），所以同一個事件每位玩家各自看到自己的語言。反過來，沙盒清單的關鍵字比對是在**伺服器端**跑的（`getItemNameFromFullType` 吃伺服器語言），手打翻譯名可能不命中——照設計流程用清單產生器複製 ID 則不受影響。

## [42.20.2-0.2.0] - 2026-08-09

### 修正

- **「掃描區域上限」實際上是全服總和**：區域計數把所有線上玩家的區塊併成單一總數，於是地圖另一頭的人砍樹會害你這邊跳警告。更糟的是玩家一走動聯集就變、總數在上限附近震盪，警告記錄反覆清除重建，永遠等不到「下一輪仍超標」那一輪——結果是無限刷警告卻從不執行清理（正式服 log 佐證：warn 571 筆、對應的 auto_clean 0 筆）。改為逐玩家分開計數。
- **區域超標時警告洗版**：舊版對區域內**每個**含該物品的區塊各發一則警告（條件只是該區塊有 1 件），玩家附近有幾個這種區塊就被洗幾行一模一樣的訊息。改為每位玩家、每種物品只發一則，且只送給觸發的那位玩家（原本會廣播給附近所有人，相鄰兩人各自超標時會互相收到對方那則）。
- **警告記錄可能無界成長**：玩家離開後不再被掃描的區塊，其警告記錄不會被回收。加上閒置年齡上限。
- **動物清理的警告與完成通知可能送不到當事人**：通知以「該群動物第一隻的位置」為圓心、半徑 30 格廣播，但動物是以最近玩家為單位分組的，該玩家可能遠在 64 格（動物掃描半徑）之外——結果動物被清掉了，飼主卻完全沒收到提示。改為直接送給該組所屬的玩家。
- **「散養上限 0 ＋ 圈養上限 80」會讓動物清理整個停擺**：舊版只檢查散養上限，為 0 就跳過整輪掃描，連圈養上限與逐群組覆寫（如 `rat=20`）都一併失效。改為只要任一處有非 0 上限就照常運作，並補上散養側缺少的「上限 > 0」檢查——否則上限 0 會被判定成「永遠超標」而把散養動物全數清光。

### 新增

- **地面物品分成兩組計數，各自套用上限**：
  - **一般**：本次遊玩中被玩家丟下的（身上有丟棄者標記）——每區塊 100、每位玩家掃描區域 400
  - **高容忍**：沒有丟棄者標記的（森林地表原生的枝條草叢，或本 MOD 安裝前就躺在地上的舊物），或列進新的「高容忍物品清單」的——每區塊 300、掃描區域 2000

  兩組獨立計數、獨立比對、獨立挑候選，因此「100 顆世界原生的原木 ＋ 150 顆某人倒的原木」只會清掉那 150 顆裡超額的部分。
- **三個新沙盒選項**：「每區塊上限（高容忍）」「掃描區域上限（高容忍）」「高容忍物品清單」。清單支援關鍵字（物品 ID／英文名／翻譯名），與保護清單同款輸入方式。與保護清單的差別：高容忍只是放寬上限，超過仍會清理；保護清單是永不清理。
- **警告顯示實際數字**：訊息帶上偵測數與當前上限（例「木板：452／上限 300」），玩家知道超出多少、清到什麼程度才會停。動物警告同樣顯示。

### 變更

- 「每區塊／掃描區域同類地面物品上限」更名為「…（一般）」，語意縮限為只計入玩家丟下的物品。**選項 ID 不變，既有伺服器設定不受影響。**
- 停用條件改為「四個上限全為 0」。舊版只檢查一般那兩項，會讓「一般設 0、高容忍非 0」的組合意外整個停擺。
- 全庫改用非遞迴排序 `Cleaner.sortSafe`，`table.sort` 呼叫數歸零。原本剩下的幾處輸入都來自 `pairs()`（雜湊順序），不會觸發 Kahlua 遞迴 quicksort 的最壞情況，但留著就是一顆定時炸彈——日後若有人把輸入改成已排序的來源就會靜默復發。現在「全庫不得出現 `table.sort(`」是一條可以直接 grep 驗證的規則。

> 技術要點：判別「世界原生 vs 玩家丟下」的完美依據其實是引擎的 `IsoWorldInventoryObject.dropTime`（預設 -1.0，只在物品落地時賦值——`IsoWorldInventoryObject.java:65,101`），原版就是靠 `dropTime > -1` 決定地上物品要不要自動移除（`IsoGridSquare.java:3284`）。但它是沒有 getter 的 public instance field，Kahlua 不暴露 → Lua 讀出來是 nil 且不報錯，因此改用本 MOD 自己在伺服器端蓋的丟棄者標記作為同語意的 Lua 版近似。「記錄物品丟棄者」關閉時會 fail-closed 退回只認清單——否則全部物品都沒有標記，會整批落進高容忍組，清理形同關閉。另新增 `scripts/smoke_scanner.lua`：用假的 PZ 全域驅動真正的掃描器跑完「掃描→警告→確認→刪除」全程，攔截 `luac -p` 抓不到的執行期錯誤（本次即靠它與跨模型審查攔下一個「首件刪除必崩」的簽章漏改）。

## [42.20.2-0.1.3] - 2026-08-08

### 修正

- **容器內有物品時無法刪除該容器**：包裹、郵差袋、鑰匙環等只要裡面還裝著東西就不顯示刪除選項，玩家得先把內容物一件件刪掉才能刪容器。現在可直接刪除，確認視窗會明確告知會一併銷毀幾件物品。
- **鑰匙環系列完全無法刪除**：所有鑰匙環都帶 `NeverEmpty` 標籤，戰利品系統生成時若內容為空會直接把它移除，因此世界上**不存在空的鑰匙環**——先前「只有空容器才可刪」的規則等於整個系列永遠擋著。

### 變更

- **手動刪除的保護規則縮減為兩項**：最愛物品、裝備／穿戴／掛載中的物品。裝著東西的容器改為允許刪除但在確認視窗警告，與原版垃圾桶可銷毀整包物品的行為一致。自動清理（地板物品）**維持**保護有內容的容器不變——無人為確認的自動行為風險不同。
- **移除「最後接觸者」追蹤，只保留「最後丟棄者」**：兩者在絕大多數情況是同一人（丟下物品的人通常就是最後碰過它的人），顯示兩行多餘。移除後同時省下每次搬移物品的網路往返，並少一個 modData 欄位——物品更容易被同型壓縮合併，存檔與封包都更小。保留的丟棄者追蹤是可靠度較高的那一個，由伺服器端直接寫入所有丟棄路徑。
- 沙盒選項「啟用接觸者追蹤」更名為「記錄物品丟棄者」（選項 ID 不變，既有伺服器設定不受影響）。

> 技術要點：`ItemTag.NEVER_EMPTY` 的語意見 `ItemPickerJava.java:1160`——生成後若容器為空即 `container.Remove(spawnItem)`。確認視窗的多行處理另有一個原版特性：`ISModalDialog:new` 會先把字面的反斜線 n 轉成真換行（:188），但 `CalcSize` 仍以字面的反斜線 n 分行（:172），導致多行文字只算一行高度；本 MOD 因此自行預留第二行的寬高。舊存檔殘留的 `MIC42_lastTouchedBy` 欄位不再讀取，無需清理。

## [42.20.2-0.1.2] - 2026-08-08

### 修正

- **鑰匙環無法刪除**：所有鑰匙環（包含純裝飾用的紀念品款，如「鑰匙環 (松樹)」「鑰匙環 (鱷魚)」）都被誤判為受保護物品而不顯示刪除選項。現在改以「容器內是否有東西」判定——空的鑰匙環可以刪除，掛著鑰匙的鑰匙圈仍受保護。

### 變更

- **刪除保護規則調整**：由「排除鑰匙環」改為「排除裝著物品的容器」。此變更同時補上一個原有漏洞——裝滿物品的背包先前可被直接刪除，現在也受保護（先清空才能刪）。

> 技術要點：所有鑰匙環變體都是 capacity 1 的 `InventoryContainer` 且共用 `base:keyring` tag（`container.txt`），無法用 tag 區分裝飾款與功能款。改用 `instanceof(item, "InventoryContainer")` 搭配 `getInventory():isEmpty()` 判定實際內容物；`getInventory()` 僅存在於 `InventoryContainer`（`InventoryContainer.java:49`），故沿用 vanilla 的 `instanceof` 慣例（`ISInventoryPane.lua:967`）而非字串比對 `getCategory()`。

## [42.20.2-0.1.1] - 2026-08-08

### 修正

- **多人伺服器上週期掃描每輪失敗**：玩家人數較多時，地板物品的週期掃描會因 Lua 堆疊溢位而中斷，導致自動清理不會執行（手動刪除、丟棄當下的即時偵測與動物清理不受影響）。改用非遞迴排序後恢復正常。

> 技術要點：Kahlua 的 `table.sort` 是遞迴 quicksort，跑在 coroutine 堆疊上（`MAX_STACK_SIZE=3000`，`Coroutine.java:16`）；輸入已接近排序時退化成 O(n) 遞迴深度，數百筆即溢位。`buildPeriodicChunks` 產生的 chunk 清單本身即有序（巢狀迴圈依 cy/cx 遞增），掃描半徑 80 時每位玩家每層樓約 441 個 chunk，多人在線穩定觸發。修法：新增 `Cleaner.sortSafe`（迭代式 bottom-up merge sort），移除 `buildPeriodicChunks` 中多餘的排序，並將候選清單與物品快取等可能較大的陣列改用之。

## [42.20.2-0.1.0] - 2026-08-08

### 新增

- **全容器刪除物品**：任何容器（背包、櫃子、車廂、地板…）右鍵即可單獨刪除或批量刪除物品，刪除前有確認視窗
  - 多人遊戲由伺服器端驗證後代為執行，一般玩家無需管理員權限
  - 保護機制：最愛物品、裝備／穿戴中、鑰匙圈一律排除
  - 伺服器可用沙盒選項 `允許手動刪除` 整個關閉
- **地板物品自動清理**：偵測範圍內堆積過量的同類地板物品並清理超額部分
  - 雙層閾值：每區塊（8×8 格）上限與掃描區域總量上限，兩層獨立判定
  - 丟棄當下即偵測（不需等待週期掃描），並有週期掃描兜底
  - 整堆整堆清除：從最新丟棄的堆開始，保留較早存在的物品
  - 可設定永不清理的物品保護清單
- **動物自動清理**：範圍內同物種動物過多時自動清除超額，緩解大量動物造成的效能問題
  - 散養與圈養分開計數、各有獨立上限，皆可逐物種覆寫
  - 圈養動物預設完整保護（上限 0 ＝ 永不清理）
  - 絕對保護：命名過的、被牽抱的、掛鉤上的動物永不清除
  - 預設清理物種：老鼠、家鼠、兔子、雞（可自訂白名單）
- **警告機制**：偵測到超量時先警告，下一輪掃描仍超量才清理，給玩家自行整理的緩衝
  - 三重提示：角色頭上持續顯示的紅色警告、聊天室系統訊息、提示音效
  - 清理完成後發出綠色通知並顯示剩餘數量
- **接觸者追蹤**：記錄物品的最後接觸者與丟棄者，滑鼠停留於物品時顯示於提示資訊
- **清理紀錄**：所有警告與清理事件寫入獨立記錄檔（`Zomboid/Logs/`），包含時間、物品／物種、數量、座標與丟棄者
- **清單產生器**：遊戲內右鍵開啟的搜尋工具，可用關鍵字（名稱或 ID）搜尋物品與動物群組，挑選後複製精確值貼入沙盒選項
- **批量生成動物**：管理員測試工具，右鍵選單可一次生成指定數量的特定動物
- **沙盒選項**：14 項設定分為「一般／物品／動物」三個分頁
- **四語系支援**：英文、繁體中文、簡體中文、日文
